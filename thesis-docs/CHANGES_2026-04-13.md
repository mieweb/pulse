# Thesis Changes — 2026-04-13

All changes made in this session to wire runtime evidence from the live deployed PulseVault server into the thesis.

---

## Summary

| File | Type | Change |
|---|---|---|
| `PurdueThesis/ap-validation-evidence-packet.tex` | Modified | Upgraded from code-inspection-only to dual-evidence (code + runtime) for all 4 validation families |
| `PurdueThesis/ch-system-design-validation.tex` | Modified | Added runtime evidence summary paragraph after each of the 4 validation interpretation blocks |
| `PurdueThesis/ap-screenshot-catalog.tex` | Modified | Added Runtime Log Evidence Assets section with R1–R16 catalog table |
| `PurdueThesis/ap-professor-review-packet.tex` | **New file** | Full professor review appendix: what-changed summary, delta table, claim/evidence/non-claim matrix, known limits, deferred empirical plan, 3-question feedback block |
| `PurdueThesis/thesis.tex` | Modified | Added `\include{ap-professor-review-packet}` after `ap-deployment-operations-notes` |
| `thesis-docs/YOUR_MANUAL_TODO_FOR_FIGURES_AND_FINISH.md` | Modified | Checked off completed evidence items; updated remaining manual-only list |

---

## Evidence Archive

**Location:** `thesis-docs/pulsevault-evidence/2026-04-13/` (22 files, extracted from `pulsevault-evidence-2026-04-13.tar.gz`)

| File | Key Data Point |
|---|---|
| `upload_finalize_success.log` | TUS POST 201 + Location: .../7d5e44dd-... + tus-resumable: 1.0.0 |
| `upload_finalize_negative.log` | HTTP 412 finalize rejection before storage mutation |
| `transcode_lifecycle.log` | 9 jobs × 5 renditions, 5.44–13.27s completion range |
| `transcode_artifacts_listing.txt` | 25 HLS video dirs, 919.6 MB, Jan–Apr 2026 |
| `token_access_positive.log` | HTTP 200 + application/vnd.apple.mpegurl |
| `token_access_negative.log` | HTTP 401 "No token provided" + HTTP 401 "Token expired" |
| `nginx_traffic_sample.txt` | Real HLS segment delivery (200s, sub-second rt) from production dashboard |
| `metrics_snapshot.txt` | CPU 14346s, memory 57 MB, eventloop p99 0.010s |
| `prometheus_targets.txt` | pulsevault-backend health=up, scrape 7.6ms |
| `loki_query_sample.txt` | Labels confirmed; promtail 92 active files; live audit chain entries |
| `audit_log_sample.txt` | 89 log files Jan–Apr 2026 with prevHash/hash chain |
| `audit_chain_verification.txt` | verifyChain() = true |
| `redis_info.txt` | Redis 7.4.7, uptime 33 days, queue depth 0, dbsize 3 |
| `service_health_table.txt` | All 9 services Up 4 weeks |
| `docker_stats.txt` | Per-container CPU/memory/network utilization |
| `qr_endpoint_response.txt` | /qr/configure-destination HTTP 200 + pulsecam:// deeplink + tokenId |
| `qr_deeplink_headers.txt` | HTTP 200, Content-Type: application/json, Content-Length: 1436 |
| `storage_stats.txt` | 919.6 MB videos, 25 dirs, 1.5 MB audit, 89 files, 78% of 32 GB used |
| `stack_state.txt` | docker compose ps snapshot |
| `service_logs_snapshot.log` | nginx HLS serving log sample |
| `backend_worker_logs.txt` | Combined backend + worker log output |
| `metrics_full_snapshot.txt` | Full 407-line Prometheus metrics dump |

---

## File-by-File Change Detail

### `ap-validation-evidence-packet.tex`

- Updated `\ProvidesFile` date to 2026-04-13.
- Rewrote Purpose section to describe dual-evidence approach (code inspection + runtime supplement).
- Added `\subsection{Code-Inspection Evidence}` heading to each of the 4 sections.
- Added `\subsection{Runtime Evidence Supplement (2026-04-13)}` to all 4 sections with verbatim protocol/log excerpts using real values from the archive.

### `ch-system-design-validation.tex`

Four `\textbf{Runtime evidence summary (2026-04-13):}` paragraphs added, one after each validation family's `\textbf{Validation interpretation:}` block:
- After upload/finalize interpretation: TUS 201 session + 204 OPTIONS + 412 guard evidence.
- After transcode interpretation: 9 jobs, 5.44–13.27s, 25 HLS dirs, 919.6 MB.
- After token/access-control interpretation: 401 no-token + 401 expired + 200 valid + nginx delivery logs.
- After observability interpretation: Prometheus up 7.6ms + metrics counters + verifyChain=true + all services Up 4 weeks.

### `ap-screenshot-catalog.tex`

Added `\section{Runtime Log Evidence Assets}` at end of file with a 16-row catalog table (R1–R16) mapping each evidence artifact to its validation family appendix reference and key data point.

### `ap-professor-review-packet.tex` (new)

New chapter `PROFESSOR REVIEW PACKET` with 4 sections:
1. What Changed (scope adjustment, chapter delta table, evidence layer added).
2. Claim vs. Evidence vs. Non-Claim Matrix (11 rows covering architecture through deferred items).
3. Known Limits and Deferred Empirical Plan (4 limits + 4-phase plan).
4. Targeted Feedback Questions (3 specific questions for committee review).

### `thesis.tex`

Added one line after `\include{ap-deployment-operations-notes}`:
```latex
\include{ap-professor-review-packet}
```

### `thesis-docs/YOUR_MANUAL_TODO_FOR_FIGURES_AND_FINISH.md`

- Checked off all runtime evidence items (Section 2) with capture date.
- Checked off all review packet items (Section 4) with file references.
- Retained unchecked: `\approvedby{}`, QR scan screenshot, ops screenshots, sending to professors.

---

## What Remains Manual-Only

- `\approvedby{...}` — program office confirmation required.
- QR scan-result screenshot — needs phone with Pulse app.
- Ops screenshots (Grafana/Prometheus UI) — needs SSH tunnel + browser.
- Emailing professors — manual send.
